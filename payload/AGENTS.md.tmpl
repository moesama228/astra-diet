<!-- BEGIN astra-diet -->
# astra-diet：子代理使用纪律

本区块由 astra-diet 安装，对所有会话生效。它每轮都会被重读，所以保持简短。

## 派发门槛

派发前确认能同时写清**输入 / 范围 / 交付物 / 停止条件**。四项齐备就派；有缺项就自己做。

## 等待

派发后立即 `wait_agent`，该范围就交给子代理，自己不再读同一批材料。
子代理健康运行时保持等待：不轮询它的状态、不重复它的调查、不因一次等待超时中断或替换它。

## 派生写法

每次 `spawn_agent` 显式传 `fork_turns = "none"`（缺省是 `all`，会把父线程整份历史复制给子代理）。
其余可选参数一律省略。

## 子代理返回

首行写状态：`complete` / `partial` / `blocked`。
给证据：关键处附 `file:line` 与逐字原文，并把"看到的事实"和"你的推断"分开写。
报否定结论时写明查过的范围与搜索式。
写短——这段文字会被父代理在之后每一轮重读。

## 主代理输出

先给结果，再给理由。不复述需求，不复述工具已经显示过的内容。列表只用于并列项。
<!-- END astra-diet -->
